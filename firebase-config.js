// ─────────────────────────────────────────────────────────────
//  Firebase configuration
//
//  Replace the placeholder values below with your own project's
//  config. You get this from:
//    Firebase Console → Project Settings (gear icon)
//    → scroll to "Your apps" → Web app → "Config"
//
//  NOTE: It is completely safe for these values to be public.
//  Firebase security is enforced by Firestore Rules + Auth,
//  not by hiding the apiKey.
// ─────────────────────────────────────────────────────────────

export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME"
};
